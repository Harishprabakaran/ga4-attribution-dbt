
# Name your project! Project names should contain only lowercase characters
# and underscores. A good package name should reflect your organization's
# name or the intended use of these models
name: 'my_new_project'
version: '1.0.0'
config-version: 2

# This setting configures which "profile" dbt uses for this project.
profile: 'default'

# These configurations specify where dbt should look for different types of files.
# The `model-paths` config, for example, states that models in this project can be
# found in the "models/" directory. You probably won't need to change these!
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"  # directory which will store compiled SQL files
clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"


# Configuring models
# Full documentation: https://docs.getdbt.com/docs/configuring-models

# In dbt, the default materialization for a model is a view. This means, when you run 
# dbt run or dbt build, all of your models will be built as a view in your data platform. 
# The configuration below will override this setting for models in the example folder to 
# instead be materialized as tables. Any models you add to the root of the models folder will 
# continue to be built as views. These settings can be overridden in the individual model files
# using the `{{ config(...) }}` macro.

# Model configurations
models:
  attribution_pipeline:
    # Staging models - views for development, tables for production
    staging:
      +materialized: view
      +docs:
        node_color: "lightblue"
    
    # Intermediate models - ephemeral for efficiency
    intermediate:
      +materialized: ephemeral
      +docs:
        node_color: "orange"
    
    # Mart models - tables with optimization
    marts:
      +materialized: table
      +docs:
        node_color: "green"
      +partition_by:
        field: event_date
        data_type: date
      # Cluster by user for attribution queries  
      +cluster_by: ["user_pseudo_id"]        

# Variables for configuration
vars:
  # Attribution configuration
  attribution_lookback_days: 30
  session_timeout_minutes: 30
  conversion_events: ['purchase', 'add_to_cart']
  
  # Data source configuration  
  ga4_project_id: 'bigquery-public-data'
  ga4_dataset_id: 'ga4_obfuscated_sample_ecommerce'
  ga4_table_pattern: 'events_*'

  # Date range for initial load (last 90 days)
  start_date: '2020-01-01'
  end_date: '2020-12-31'
     